module compressor_4_2 (
    input  x1, x2, x3, x4,
    input  Cin,
    output Sum,
    output Carry,
    output Cout
);

    wire [2:0] temp;

    assign temp = x1 + x2 + x3;
    assign {Cout, temp[0]} = temp;

    assign {Carry, Sum} = temp[0] + x4 + Cin;

endmodule